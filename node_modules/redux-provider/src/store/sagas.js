// https://github.com/benmosher/eslint-plugin-import/pull/794
// eslint-disable-next-line import/order
import { all /*, fork */ } from 'redux-saga/effects'

import { requests } from '../request/state'

/*
  Root watch "loop" for request handling
 */
export function* rootSaga() {
  yield all([...requests])
}
