// @flow

import { get, isEmpty, identity } from 'lodash'

import { StateSlice, Action } from './types'

export const statesReducer = (
  state: StateSlice,
  action: Action,
) => {
  const states = get(action, identity)

  if (isEmpty(states)) {
    return state || {}
  }

  return states
}
