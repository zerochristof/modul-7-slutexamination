import { Response } from '../../../request/types'

import React from 'react'

export type ComponentType = typeof React.Component

export interface Payload {
  +result: Object | Array | string | number | null,
  +states: StateSlice,
}

export interface Action {
  +type: string;
  +payload: Object<Payload>;
  +component: ComponentType | Array<ComponentType>;
}

export type StateType = string
export type State = Response
export type StateSlice = { [StateType]: State }
